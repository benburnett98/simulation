package cellsociety.view;

import cellsociety.view.Tile;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;


/*
public class testTile {

    private Tile myTile;

    /**
    @Test
    public void testTileTypeChange() {
        myTile = new Tile(0,0, 0, 40);
        myTile.changeTile(1);
        assertEquals(1, myTile.getType());
    }

    @Test
    public void testTileColorChange() {
        Tile tt = new Tile(0,0, 0, 40);
        myTile = new Tile(0,0, 0, 40);
        myTile.changeTile(1);
        assertNotEquals(myTile.getMyTile(), tt.getMyTile());

    }
}
 */